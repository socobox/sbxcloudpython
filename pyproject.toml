[project]
name = "sbxpy"
version = "0.8.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.7.3",
    "pydantic>=2.1.0",
    "redis>=4.5.2",
]
